@.claude/memories.md

# traffic-game

Browser prototype: draw streets freehand, then simulate traffic on them.

```bash
npm run dev      # vite dev server on :5173
npm run build    # tsc --noEmit && vite build
```
